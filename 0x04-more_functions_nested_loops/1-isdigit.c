#include "main.h"

/**
 * main - checks if  for a digit (0 through 9).
 * Return 1 if c is a digit otherwise return 0
 */
int _isdigit(int c)
{

	int c;

	if(isdigit(c))
		
		c = 1;
	else
		c = 0;

	return up;
}
